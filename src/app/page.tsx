export default function HomePage() {
  return (
    <main className="system-shell">
      <section className="system-shell__frame" aria-label="POLNAREFF SYSTEM">
        <p className="system-shell__eyebrow">POLNAREFF SYSTEM // LOCAL COMPUTING ENVIRONMENT</p>
        <h1>Renderer bootstrap pending.</h1>
        <p className="system-shell__copy">
          The spatial runtime will mount here during Stage 1.
        </p>
      </section>
    </main>
  );
}
